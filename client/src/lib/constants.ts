/** How long a soft-deleted transaction stays recoverable in "Recently deleted"
 *  before the server purges it for good. Mirrors TRASH_RETENTION_DAYS on the server. */
export const TRASH_RETENTION_DAYS = 30;
